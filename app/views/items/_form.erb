<%= form_for :item do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :description %>
    <%= f.text_area :description %>
    <%= f.label :conditio %>n
    <%= f.test_field :condition %>
    <%= f.select :gear_list_id, @gear_lists.collect {|g| g.name, g.id} %>
 
    <%= f.submit %>
<% end %>